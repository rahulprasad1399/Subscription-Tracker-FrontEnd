import { ServiceImagePipe } from './service-image.pipe';

describe('ServiceImagePipe', () => {
  it('create an instance', () => {
    const pipe = new ServiceImagePipe();
    expect(pipe).toBeTruthy();
  });
});
